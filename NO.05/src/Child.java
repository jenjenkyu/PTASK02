// extends - extend the functionality of the parent class to the subclass.
// which mean u can use all the functionality 
// thru other subclass by extending it to there super class
public class Child extends Parent {
	
	//Child Constructor
	public String Child() {
		//returns String 
		return "I am a Child Class" ;
	}
}
